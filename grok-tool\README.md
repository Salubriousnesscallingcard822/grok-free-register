# Grok Tool wrapper notes

This folder intentionally stays thin.
Runtime lives in `../token_manager`.

Start:

```powershell
powershell -ExecutionPolicy Bypass -File ..\start-grok-tool-windows.ps1
```
